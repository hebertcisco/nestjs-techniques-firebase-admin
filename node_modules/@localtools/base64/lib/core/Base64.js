"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Base64 = void 0;
const buffer_1 = __importDefault(require("buffer"));
class Base64 {
    static encode(args) {
        const { text } = args;
        if (!text) {
            throw new Error('Invalid text');
        }
        return buffer_1.default.Buffer.from(text).toString(args.encoding || 'base64', args.start, args.end);
    }
    static decode(args) {
        const { base64Text } = args;
        if (!base64Text) {
            throw new Error('Invalid text');
        }
        return buffer_1.default.Buffer.from(base64Text, args.fromEncoding || 'base64').toString(args.outputEncoding || 'utf8');
    }
}
exports.Base64 = Base64;
exports.default = Base64;
