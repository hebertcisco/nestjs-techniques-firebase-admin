import moment from 'moment';
import { RelativeTime } from '../interfaces/RelativeTimeInterface';
import type { RelativeTimeOptions } from '../types/RelativeTimeOptions';
export declare class DateHandle implements RelativeTime {
    private day;
    constructor();
    dayOfWeek(thisDay: number): boolean;
    isSaturday(): boolean;
    isSunday(): boolean;
    isMonday(): boolean;
    add(date: Date, options: RelativeTimeOptions): Date;
    difference(start_date: Date, end_date: Date): Date;
    static future(amount?: moment.DurationInputArg1, unit?: moment.unitOfTime.DurationConstructor | undefined): moment.Moment;
    static futureFully(amount?: moment.DurationInputArg1, unit?: moment.unitOfTime.DurationConstructor | undefined): moment.Moment;
    static past(amount?: moment.DurationInputArg1, unit?: moment.unitOfTime.DurationConstructor | undefined): moment.Moment;
    static pastFully(amount?: moment.DurationInputArg1, unit?: moment.unitOfTime.DurationConstructor | undefined): moment.Moment;
    static date: {
        y: string;
        m: string;
        d: string;
        now: string;
        nowFully: string;
        h: string;
    };
    static yesterday: () => string;
}
