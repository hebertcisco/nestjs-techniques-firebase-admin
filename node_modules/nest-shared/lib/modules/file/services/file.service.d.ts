/// <reference types="node" />
import type { Request } from 'express';
import type { URL as UrlType } from 'url';
import type { FileServiceArgs, FileType } from '../types/file.type';
import type { FilesInterceptorInterfaceArgs } from '../interfaces';
export declare class FileService {
    private args;
    private s3;
    constructor(args: FileServiceArgs);
    genFileName: (req: Request, { originalname }: FileType, callback: (error: Error | null, filename: string) => void) => void;
    filesInterceptor(args: FilesInterceptorInterfaceArgs): import("@nestjs/common").Type<import("@nestjs/common").NestInterceptor<any, any>>;
    getS3FileURL(req: Request | any): UrlType | null;
    getS3FileKey(req: Request): string | null;
    getSignedUrl(key: string, expires: number, bucket?: string): UrlType;
    handleSignedUrl(bucket: string, key: string, expires: number): UrlType;
    parseFileURL(avatar: string, bucket: string, expires?: number): string;
}
export default FileService;
