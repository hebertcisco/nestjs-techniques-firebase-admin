import NumberLength from '../../core/NumberLength';
export declare class Configuration {
    private _minLength;
    private _maxLength;
    private _timestampBased;
    constructor();
    reset(): Configuration;
    setLength(length: number): Configuration;
    setMinLength(length: number): Configuration;
    getMinLength(): NumberLength;
    setMaxLength(length: number): Configuration;
    getMaxLength(): NumberLength;
    timestampBased(): Configuration;
    isTimestampBased(): boolean;
    private _validateLength;
    private _minIsGreaterThanMax;
}
export default Configuration;
